process {
    container = 'quay.io/nextflow/bash'
}

profiles {
    test_profile {
        params {
            counts_filters = [
                [:],
                [
                    min_cpgs: 5,
                    max_insert_size: 150
                ]
            ]
            qc_summary_config = [:]
        }
    }
}